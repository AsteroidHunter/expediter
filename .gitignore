node_modules

# npm lockfile — repo uses bun.lock as the source of truth
package-lock.json

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# internal docs, extremely sensitive
expediterwiki

# python venv
.venv

# Claude Code local-only settings
.claude/settings.local.json

# Dev-only fixtures, scratch, and other untracked working files
internal

# Local-only Claude Code workspace (skills, hooks, settings)
.claude/

# Local-only editor settings
.vscode/
